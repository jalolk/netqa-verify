[pytest]
testpaths = tests
pythonpath = .
addopts = -ra --strict-markers
markers =
    functional: core behaviour of the running topology
    parity: identical assertions executed through every backend
    regression: re-checks core paths after configuration change
    performance: measures timing characteristics
    security: verifies traffic filtering behaves as intended
    slow: takes appreciable wall-clock time
